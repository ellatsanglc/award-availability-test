// ── Backend URL ──────────────────────────────────────────────────────────────
// Update this to your ngrok static domain after running the setup steps.
// Leave as-is when running locally (localhost uses relative URLs automatically).
window.BACKEND_URL = 'https://spotter-portly-onto.ngrok-free.dev';
